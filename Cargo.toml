[package]
name = "winage"
version = "2.0.0"
authors = ["Theron <tspiegl@gmail.com>"]
edition = "2021"
rust-version = "1.74"

[dependencies]
age = { version = "0.12", features = ["armor", "cli-common", "plugin", "ssh"] }
rand = "0.8"
chrono = "0.4"

[lib]
crate-type = ["staticlib"]
